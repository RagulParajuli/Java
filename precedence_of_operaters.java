public class precedence_of_operaters {
    public static void main(String[] args) {
        int a=6*5-34/2;
        System.out.println(a);
     // in  mathematical, boardmas rule is applied
    // but in java precedence & Associativity is used for mathematical calculation
    // Associativity goes from left to right
    // in precedence, * && / have same weight but more than + && -.
    // we can usse bracket to change the flow of Associativity.
    }
}
